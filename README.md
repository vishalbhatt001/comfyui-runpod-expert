# ComfyUI + RunPod Expert

A Claude Code Skill: everything learned building four ComfyUI + RunPod pipelines (`minimax-h3-studio`, `wan22-music-video`, `ace-music-studio`, `music-video-generator`) — verified model/node details, Docker build fixes, RunPod API gotchas, cost-estimation method, and Apple Silicon limits.

The actual content is [`SKILL.md`](SKILL.md). This repo is symlinked into `~/.claude/skills/comfyui-runpod-expert/` so Claude Code loads it automatically for future ComfyUI/RunPod work — updated and pushed here every time something new gets debugged or verified.
